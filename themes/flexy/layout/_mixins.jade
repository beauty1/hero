mixin page(title)
  if title == page.title.toLowerCase()
    include= title

mixin archive(post)
  li
    a(href= config.root + post.path) #{post.title}
    |  - #{ full_date(new Date(post.date), 'dddd, YYYY, MMM Do ') + full_date(new Date(post.date), 'H:mm')}